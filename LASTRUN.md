This round applied `jobSearch-round2.patch`, fixed all bugs it flagged, removed dead code, closed the Greenhouse `content` gap with a real live capture, and closed both previously-documented coverage gaps. Every fix was test-first: a failing regression test confirmed first, then the fix, then confirmed green. Nothing was committed, per instructions.

**Phase 0 — patch applied.** `git apply` failed on the raw patch (missing trailing newline + CRLF/LF mismatch between the code hunks and the doc hunks), fixed with `tr -d '\r'` + a trailing newline, exactly as the work order predicted. After applying, 2 of the patch's own new tests failed (`tests/unit/aiDraftPipeline.test.ts`'s `prefilterJob` tests) — not because of anything in the fix list, but because that file's env-isolation `KEYS` list was missing `PREFILTER_PROVIDER_FORMAT`/`PREFILTER_BASE_URL`/`PREFILTER_MODEL`, so this repo's real `.env` (which has `PREFILTER_PROVIDER_FORMAT` set to run the prefilter tier on Groq) leaked into the test and broke its anthropic-shaped assumptions. Fixed by bringing that file's `KEYS` list in line with `resolveProvider.test.ts`/`callAiModel.test.ts`, which already had the complete list. Confirmed green before starting Phase 1, per the ground rule against stacking new work on a red baseline.

**Phase 1 — all five bugs fixed, test-first:**
- **TherapyNotes**: now filters by `matchesAnyTitle` like every other fetcher, and `location` builds from the real top-level `city`/`state`/`country` fields instead of a nonexistent `job.location` object. Worth knowing: as of this fix, none of the 3 postings currently on the real captured board actually match `SEARCH_TITLES` — a real, current fact about that board, not a test artifact.
- **Workable cross-search**: `company` now reads `job.company.title` (not `.name`), `location` builds a joined string from the real `{city, subregion, countryName}` object (skipping empty/null parts), `postedAt` reads `job.updated` (not `job.updatedAt`, kept as a harmless fallback).
- **RemoteOK**: `job.epoch` (Unix seconds) is now converted to milliseconds at the fetcher call site before it ever reaches `daysOld`/`daysAgoLabel` — this was the bug actually costing real leads (postings silently vanishing). `daysOld`/`daysAgoLabel` themselves were left untouched.
- **USAJOBS**: salary formatting now uses `remuneration.Description` (e.g. `"Per Year"`) instead of the raw `RateIntervalCode` abbreviation (e.g. `"PA"`).
- **`extractUnconfirmedTerms`**: no longer leaks the literal word "unconfirmed" into its output — the `### Unconfirmed` heading line is stripped before tokenizing, inside the function itself (`significantWords` was left untouched since it's shared with the template-kit engine).

**Phase 2 — `stripHtml` removed.** Confirmed zero call sites in `check-jobs.ts`'s control flow via grep; deleted the function and its test file.

**Phase 3 — Greenhouse `content` gap closed with a real live capture.** `curl`'d `https://api.greenhouse.io/v1/boards/impiricus/jobs?content=true` for real (network access confirmed available). Found that `job.content` is HTML markup that's itself HTML-entity-encoded — a literal `<p>` tag arrives as the text `&lt;p&gt;` — so a small local `stripGreenhouseContent` helper decodes entities before stripping tags, not after. `tests/fixtures/greenhouse-response.json` was replaced with a fresh real capture (both jobs, including the real QA Automation Engineer posting whose real content contains "3+ years of hands-on proven experience" and "Remote, USA"). `yearsRequired`/`workArrangement` now populate correctly from real Greenhouse data.

**Phase 4 — both coverage gaps closed:**
- **`AI_PIPELINE_ENABLED=true`**: new dedicated file `tests/integration/main-ai-pipeline.test.ts` (2 tests: successful draft+commit+attachment, and prefilter-rejects). Discovered along the way that `AI_PIPELINE_ENABLED` is a module-level constant evaluated once at import time, so a `beforeEach` env-var set has no effect on an already-imported module — worked around with `vi.resetModules()` + a dynamic `import()` per test, in its own file so the reset doesn't disturb `main.test.ts`'s other tests. Documented in `TESTING.md` and `CONTEXT.md` as a real gotcha for next time.
- **`safely()`/`safelyValue()`/`safelyRun()`**: new test in `tests/integration/main.test.ts` makes Adzuna's fetch call genuinely reject while all 10 other sources resolve normally; confirms `main()` still completes, the alert email still sends with the other sources' postings, and `console.error` was called with `"Adzuna failed:"`.

**Final numbers, all confirmed by real runs:** `npm test` — 198 Vitest tests, 35 files, all passing. `npm run test:email` — 5 Playwright tests, 2 files, all passing. `npm run test:coverage` — 99.07% statements / 89.03% branches / 100% functions / 99.07% lines on `check-jobs.ts` (up from 96.09%/88.54%/100%/96.09% at the start of this round).

**Docs updated:** `TESTING.md` and `CONTEXT.md` rewritten with real numbers and the fixed-quirks list moved out of "documented, not yet fixed." `HANDOFF.md`'s "Where things stand" and "Open items" updated to reflect the four bugs and the resilience layer as closed; genuinely-still-open items (SMS/push, Statheros fragility, the Sonnet-polish-pass idea, treeLine items) left untouched. `CLAUDE.md` (the round-2 work order) deleted now that everything it asked for is done.

**Nothing left unfixed from this round's scope.** Nothing was committed — the working tree is left for review.

## 2026-07-15 — Round 3: field-mapping & coverage audit

**Verify-first check:** both bugs CLAUDE.md asked to "verify are fixed" (`SEARCH_TITLES` missing "Quality Assurance Engineer" phrasing; `fetchTherapyNotesJobs` not mapping `telecommuting` into `workArrangement`) were **already fixed** in the working tree — but `tests/fetchers/therapynotes.test.ts` still had a stale test asserting the real fixture returns `[]`, written before those fixes landed. Updated that assertion to the correct current behavior (1 matching job, `workArrangement: "remote"`) rather than re-doing already-done work. Lesson for next round: run the suite for real before trusting a "this bug is unresolved" claim in a work order — a stale test can look like an open bug from the outside.

**Audit — all four suspects fixed, test-first, each grounded in a real live capture:**
- **`fetchSoltechJobs`** (highest priority): now maps `location` from the RSS feed's real `job:locationCity`/`job:locationState` namespaced tags (confirmed via live capture of `soltech.hire.trakstar.com/jobfeeds/soltech`), and calls `extractWorkArrangement()` on the already-captured `description`. The feed's own `job:locationCountry` tag is unreliable (held a zip code in the real capture) and deliberately not mapped — noted in `CONTEXT.md`.
- **`fetchTitleSearchJobs`** (Workable cross-search): now maps a real `job.workplace` field (`"remote"`/`"hybrid"`/`"on_site"`) into `workArrangement`, confirmed via a live multi-query capture. Different field from the account-widget endpoint's `telecommuting` boolean — same vendor, two endpoints, two different remote signals, consistent with the round-2 lesson not to assume schema parity.
- **`fetchQuarterhillJobs`**: `description` does exist on the real API (previous assumption that it didn't was wrong) — now captured, feeding both `yearsRequired` and `workArrangement`. Found a genuine priority-collision bug in the process: a real live posting explicitly marked `location_name: "Remote - US"` has description text containing "onsite" (checked first by the shared `extractWorkArrangement`) next to an unrelated "remote locations" mention (travel sites, not work arrangement) — scanning the description naively the same way other fetchers do would have flipped this specific remote job to "onsite" and gotten it silently dropped. Fixed by checking `location_name` for "remote" first, falling back to `extractWorkArrangement(description)` only when `location_name` doesn't decide it.
- **`fetchLeverJobs`**: now maps `job.workplaceType` into `workArrangement`, mirroring Ashby's pattern. `LEVER_COMPANIES` is still empty (no live board), so built from Lever's documented public API shape like the rest of that fixture — inert today, cheap fix while nothing depends on it yet.

**Cross-cutting title coverage sweep:** queried every `SEARCH_TITLES` entry against Workable's cross-search endpoint live (~240 unique real titles returned), diffed all of them against `matchesAnyTitle`. Added six new real, current, phrasing-gap entries: `"Software Engineer in Test"`, `"Software Tester"`, `"Software Testing Engineer"`, `"QA Architect"`, `"Test Automation Specialist"`, `"Automation Testing Engineer"` — new `tests/unit/matchesAnyTitle.test.ts` covers all six plus two real titles left deliberately unmatched (punctuation-broken, e.g. `"Software Quality Assurance - Engineer"`) rather than chased with ever-narrower phrase entries. Also spot-checked Greenhouse, Ashby, RemoteOK, Adzuna, USAJOBS, SOLTECH, TherapyNotes, and Quarterhill's live boards directly — no other current title gaps found (most had zero or one QA/SDET-matching posting live at capture time).

**Cross-cutting freshness gate check:** re-verified every fetcher's `postedAt` field name against a real live capture (not fixture assumptions) — TherapyNotes (`published_on`/`created_at`), SOLTECH (`pubDate`, RFC-822 format), Workable search (`updated`), Greenhouse (`updated_at`), Ashby (`publishedAt`), RemoteOK (`date`/`epoch`), Adzuna (`created`), USAJOBS (`PublicationStartDate`, including its unusual 4-digit fractional-second format), Quarterhill (`posted_date`). All parse cleanly via `daysOld`/`new Date()`, no `Infinity`/`NaN` surprises found. Statheros has no `postedAt` field at all, which is by design (already documented as fine — `isFreshJob` treats `undefined` as always-fresh). Lever's `createdAt` left as documented-shape-only, consistent with its workArrangement fix, since there's still no live board to capture from.

**Already-clean sources reconfirmed, not re-litigated:** Greenhouse, Ashby, Adzuna, USAJOBS, Statheros, RemoteOK — all still hold from round 2's findings; nothing new needed.

**Final numbers, all confirmed by real runs:** `npm test` — 210 Vitest tests, 36 files, all passing (up from 198/35). `npm run test:email` — 5 Playwright tests, 2 files, all passing. `npm run test:coverage` — 99.02% statements / 89.14% branches / 100% functions / 99.02% lines on `check-jobs.ts`.

**Docs updated:** `CONTEXT.md`'s gotchas section and `TESTING.md`'s "Real quirks discovered" section both got new entries for every fix above, following the existing structure/tone. `CLAUDE.md` (this round's work order) deleted now that everything it asked for is done.

**Nothing left unfixed from this round's scope.** Nothing was committed — the working tree is left for review.
