{
  "name": "therapynotes-job-checker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "tsx check-jobs.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:email": "playwright test",
    "test:all": "npm run test && npm run test:email"
  },
  "dependencies": {
    "resend": "^4.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0",
    "@vitest/coverage-v8": "^2.1.0",
    "@playwright/test": "^1.48.0"
  }
}
